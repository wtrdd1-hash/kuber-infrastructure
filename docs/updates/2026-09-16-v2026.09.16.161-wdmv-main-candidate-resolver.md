# v2026.09.16.161 — WDMV exact-main candidate resolver

## Internal / GitHub update

- Root cause: `Auto Reconcile Woldeok Moneyverse` accepted only successful `Build Test Candidate` runs whose event was `push`.
- The application branch-drain automation intentionally dispatches the final `main` candidate with `workflow_dispatch -f promote=true`; a fully successful exact-main candidate was therefore invisible to infrastructure reconciliation.
- Fix: accept successful `push` or `workflow_dispatch` Test Candidate runs only when `head_sha` exactly equals the current application `main` SHA.
- Safety: no older successful candidate can be selected, pull-request candidates remain excluded, and the existing active-PR preservation guard remains unchanged.
- Release order is unchanged: exact-main candidate -> isolated Test manifest -> public exact-SHA/Test DB smoke -> production-ready signal -> Production manifest -> public Production smoke.

## Target application release

Current application `main` at diagnosis: `7acc3c02e4fc015d6800f2d5a7e51180f2dc02a5`.

## Validation

1. Validate GitOps rendering and Test isolation in this branch.
2. Verify the resolver selects the current exact-main successful `workflow_dispatch` candidate and rejects stale/non-main runs.
3. Merge through PR after validation.
4. Dispatch auto-reconcile and confirm `staging/wdmv-test` pins the exact current app SHA.
5. Confirm public Test exact SHA before allowing Production promotion.
