# v2026.09.23.387 — wdmv-test local-auth E2E flag

- Scope: isolated `wdmv-test` only.
- Adds `LOCAL_AUTH_TEST_VERIFICATION_TOKEN_ENABLED=true` to the Test backend environment.
- Production manifests are unchanged.
- The application-side guard additionally requires the exact hostname `test.easy-scraping.com`, so this environment switch cannot enable token return on Production.
- Candidate SHA/image/source pins remain unchanged in this infrastructure change.
- The flag is inert until an application image containing v2026.09.23.387 or newer is deployed.
- After the branch Test image is available, backend/frontend/candidate labels and migration source must be pinned to one exact SHA before E2E QA.