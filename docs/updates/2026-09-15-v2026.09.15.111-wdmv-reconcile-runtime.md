# v2026.09.15.111 — WDMV reconcile runtime recovery

## Internal / GitHub update

- Root cause: rerunning an older `Auto Reconcile Woldeok Moneyverse` job checked out the historical workflow-run commit instead of current infrastructure `main`, then failed to push the current app test candidate with a non-fast-forward error.
- Secondary cause: the Flux root Kustomization and `wdmv-test` Kustomization both reconciled every 10 minutes while the application production test gate waited only about 15 minutes, allowing the gate to time out before the nested Flux chain completed.
- Recovery: pin checkout to infrastructure `main`, rebase on `origin/main` before automated promotion pushes, and trigger reconcile when the recovery workflow/config itself lands on `main`.
- Latency: reduce the Flux root and `wdmv-test` reconciliation intervals from 10 minutes to 1 minute. GitRepository polling remains 1 minute.
- Safety: same-SHA isolated test, backend/database catalog smoke, noindex validation, production-ready signal, production GitOps reconciliation, and public production smoke remain fail-closed and unchanged.

## Target application release

Current application `main`: `c82153917095bf380b5336ef95ad6932c9d7a295`.

## Validation / promotion order

1. GitOps validation on this branch/PR.
2. Merge recovery changes to infrastructure `main`.
3. Auto reconcile current successful application main test candidate into `staging/wdmv-test`.
4. Confirm `test.easy-scraping.com/api/version` serves the exact application SHA and backend/database smoke succeeds.
5. Build exact-SHA production images.
6. Promote exact SHA to production GitOps manifests.
7. Confirm `easy-scraping.com/api/version`, catalog, and status smoke checks.
