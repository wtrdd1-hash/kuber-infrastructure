# WDMVP production rollout — 2026-09-09

## Summary

Roll the Woldeok Moneyverse frontend to application commit `0014863950d130c430005cfba7dd0f28a3cfd062` using the Kubernetes/Flux GitOps source of truth.

## Changes

- Updated `apps/minipc/wdmvp/frontend.yaml` to image `ghcr.io/wtrdd1-hash/wdmv/frontend:0014863950d130c430005cfba7dd0f28a3cfd062-production`.
- Kept the existing runtime security context, resource limits, service wiring, and secret references unchanged.

## Validation evidence

Application repository PR #137 CI passed secret scan, lint, typecheck, build, database migrations, tests, Prisma mutation guard, and production dependency audit. The merged `main` commit passed the same CI again. The production image was successfully built and pushed by the application Deploy workflow before its legacy Docker Compose rollout step failed on the Kubernetes host.

## Deployment note

The application repository's current `.github/workflows/deploy.yml` still assumes Docker Compose on the target host. The actual production host uses Kubernetes/containerd managed by Flux, so the GitOps repository is the authoritative rollout path.

## Post-deploy checks

After Flux reconciles this commit, verify deployment rollout, pod readiness/restarts, running image, and public `/`, `/status`, `/robots.txt`, `/sitemap.xml`, and `/ads.txt` responses. Do not mark the rollout complete if any check fails.
