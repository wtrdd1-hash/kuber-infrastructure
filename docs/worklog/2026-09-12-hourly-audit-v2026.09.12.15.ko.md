# 시간별 통합·배포 점검 — v2026.09.12.15

기준일: 2026-09-12
대상: `wtrdd1-hash/Woldeok-Moneyverse-Migration`, `wtrdd1-hash/kuber-infrastructure`

## Living Project Plan 재확인

작업 전과 작업 중간에 최신 `docs/planning/PROJECT_PLAN.md`를 다시 확인했다. 현재 배포 원칙은 fail-closed다. 최신 main은 CI와 빌드를 통과해야 하고, 격리 Test에서 정확한 애플리케이션 SHA가 실제 응답하는 것이 확인되며 백엔드/DB smoke 근거가 있어야 한다. 그 뒤에만 동일 SHA를 Production으로 승격한다.

## 애플리케이션 저장소

- 점검 시작 main: `c318048d3604876a285f34dd9523bd1de1eaa03b`.
- 유용하지만 유휴 상태였던 PR #167 감사 문서를 CI 성공 후 통합했고 main은 `0f5b7ac5d196487459f8ecbf2f36ffeaad2b5122`가 됐다. 브랜치는 삭제했다.
- 오래되어 뒤처진 PR #166 개인 공간/도시 프로젝트 문서의 고유 7개 커밋은 새 `integrate/personal-spaces-v2026.09.12.15`에 최신 main 기준으로 순서대로 재적용했다. main이 다시 변경된 뒤 한 번 더 동기화했고 secret 검사, lint, typecheck, production build, PostgreSQL migration 적용, 테스트, Prisma mutation guard, production dependency audit가 모두 성공했다.
- 대체 PR #170을 병합하고 통합 브랜치를 삭제했다. 애플리케이션 main은 `26c6757852cb45be245e2c2a13eee3d859560d92`가 됐다.
- 기존 PR #166은 대체 통합을 검증한 뒤 닫았고 기존 문서 브랜치도 삭제했다.
- `docs/player-market-crafting-v2026.09.12.16`은 점검 도중에도 연속으로 커밋이 추가되고 자체 CI가 실행되고 있었으므로 활성 작업으로 분류해 보존했다.
- 런타임 PR #160, #162, #164, #165, #169는 활성 작업으로 보존했다. CI 성공만으로 운영 승인으로 보지 않았다.
- trusted-client-IP와 Economy Scenario Lab Test 후보 브랜치도 정확한 런타임 검증이 완료되지 않아 활성 상태로 보존했다.

## GitOps 저장소

- 점검 시작 main: `298699a3341b8651b2ec20d417c8a194871589b1`.
- 애플리케이션 후보 `8e0dab2094743e1ea8cc62e01ff8cd38e3229b27`은 앱 CI와 immutable Test 이미지 빌드가 성공했다.
- 자동 생성 Test 승격 PR #33은 처음에 validation이 action-required/실행 작업 없음으로 보여 게이트를 우회하지 않았다. 동일한 GitOps 후보 커밋을 사용자 소유 `integrate/wdmv-test-8e0dab-v2026.09.12.15` 브랜치로 옮기고 영문/한글 근거 기록을 추가해 정상 검증을 다시 요구했다.
- PR #34는 `Validate GitOps`를 통과해 병합됐다. GitOps main은 `b70e7a4da72d130038907027dc53bd6b98fdcf71`가 되었고 Test 백엔드·프론트엔드·마이그레이션·부트스트랩 참조는 모두 정확한 `8e0dab2094743e1ea8cc62e01ff8cd38e3229b27`을 가리킨다.
- PR #33도 이후 정상 검증이 완료되어 같은 Test 후보로 병합된 것이 확인됐다. 중복 승격 브랜치는 검증 후 삭제했다.
- 완료/구식 승격 브랜치 `promote/wdmv-production-6c4237ccb811`, `promote/wdmv-test-6c4237ccb811`, `promote/wdmv-test-8e0dab209474`를 삭제했다.
- DB 백업 PR #22는 활성 Draft로 보존했다. manifest 검증은 성공하지만 비운영 환경에서 실제 백업 생성·복구 및 백엔드 정상성 증거가 없으므로 운영 병합하지 않았다.

## Test 배포 증거

GitOps 희망 상태는 `8e0dab2094743e1ea8cc62e01ff8cd38e3229b27-test`를 가리키지만 GitOps 병합 후 직접 공개 Test를 점검한 결과:

- `https://test.easy-scraping.com/` — HTTP 200.
- `https://test.easy-scraping.com/app-api/v1/auth/providers` — HTTP 200, Discord/Google provider 응답.
- `https://test.easy-scraping.com/api/version` — 여러 차례 `{"id":"1789182149431"}`을 반환했고 필요한 정확한 SHA가 아니었다.

Test candidate 빌드는 GitHub SHA를 build ID로 주입하므로 현재 숫자 build ID는 정확한 후보가 실제 공개 Test에 서빙된 것이 아직 증명되지 않았다는 직접 근거다. GitOps 병합 자체를 배포 성공으로 간주하지 않았다.

현재 개발 호스트에는 `kubectl`, `k3s`, `flux`, kubeconfig가 없어 Pod 이미지, migration init-container 완료, backend 로그, 클러스터 서비스 상태, 클러스터 측 rollback 준비를 직접 확인할 수 없다.

**Test 게이트: 실패 / 미증명.**

## Production 상태

현재 Production GitOps는 애플리케이션 SHA `6c4237ccb811d37485fef2e65d390b936e188ccc`를 가리킨다. 공개 Production `/api/version`도 숫자 build ID `1789182149431`을 반환했고 auth-provider API는 HTTP 200이었다.

이번 점검에서는 `8e0dab...`, `0f5b7ac...`, `26c6757...` 후보를 Production으로 승격하지 않았다. `0f5b7ac...`에 대한 Production Release workflow는 정확한 SHA가 Test에서 확인되기를 기다리는 test-gate 단계에 있었고, workflow 정의상 이 단계가 성공하기 전에는 Production 이미지 빌드/게시로 진행하지 않는다.

**이번 점검 Production 승격: 없음.**

## 남은 위험/차단요인

1. 공개 Test `/api/version`이 희망 SHA를 반환하지 않아 Flux/Test 실제 반영이 증명되지 않았다.
2. 승인된 개발 호스트에 클러스터 도구/자격증명이 없어 migration 완료와 backend 로그를 직접 확인할 수 없다.
3. CI가 성공한 런타임 PR도 Test 게이트 전에는 Production으로 승격할 수 없다.
4. DB 백업 PR #22는 실제 백업·복구 리허설 근거가 필요하다.
5. 새 `v2026.09.12.16` 마켓플레이스/제작 기획은 점검 도중 계속 변경된 활성 작업이므로 다음 점검에서 다시 분류해야 한다.

`main`에 force-push는 하지 않았다.
