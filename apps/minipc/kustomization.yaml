# Workloads for the minipc cluster. Empty until the services being migrated
# from the machine's previous life arrive here one at a time.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources: []
