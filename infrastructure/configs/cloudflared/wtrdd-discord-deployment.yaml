# Generated by cf-tunnel -- edits here are lost on the next add or remove.
#
# Connector for the "wtrdd-discord" tunnel. It dials out to Cloudflare and holds the
# connection open, so nothing is forwarded inward and there is no address for
# the internet to reach. Losing a node costs the replicas on it and nothing
# else; the survivors keep their connections.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: cloudflared-wtrdd-discord
  namespace: cloudflared
  labels:
    app.kubernetes.io/name: cloudflared
    app.kubernetes.io/instance: wtrdd-discord
  annotations:
    cf-tunnel/id: "9e0dad42-3270-4165-af3e-be2abd0fb670"
spec:
  replicas: 2
  selector:
    matchLabels:
      app.kubernetes.io/name: cloudflared
      app.kubernetes.io/instance: wtrdd-discord
  template:
    metadata:
      labels:
        app.kubernetes.io/name: cloudflared
        app.kubernetes.io/instance: wtrdd-discord
    spec:
      affinity:
        podAntiAffinity:
          # Preferred, not required: on a single node a required rule would
          # leave the second replica Pending until another node exists.
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 100
              podAffinityTerm:
                topologyKey: kubernetes.io/hostname
                labelSelector:
                  matchLabels:
                    app.kubernetes.io/name: cloudflared
                    app.kubernetes.io/instance: wtrdd-discord
      securityContext:
        runAsNonRoot: true
        runAsUser: 65532
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: cloudflared
          image: cloudflare/cloudflared:2026.8.2
          args:
            - tunnel
            # The image tag is the version pin; letting the binary replace
            # itself would put the running code out of step with git.
            - --no-autoupdate
            - --metrics=0.0.0.0:2000
            - run
          env:
            - name: TUNNEL_TOKEN
              valueFrom:
                secretKeyRef:
                  name: cloudflared-wtrdd-discord-token
                  key: token
          ports:
            - name: metrics
              containerPort: 2000
          livenessProbe:
            httpGet: { path: /ready, port: metrics }
            initialDelaySeconds: 10
            periodSeconds: 15
            failureThreshold: 3
          readinessProbe:
            httpGet: { path: /ready, port: metrics }
            periodSeconds: 10
          resources:
            requests: { cpu: 50m, memory: 64Mi }
            limits:   { memory: 256Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: [ALL]
